#include "main.h"

/**
 *_abs - checking
 * @n: integer
 * Description: A function returns absolute value of a number
 * Return: Absolute value of number n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
