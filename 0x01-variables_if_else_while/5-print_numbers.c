#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num_list;

	num_list = 0;
	while
		(num_list <= 10)
		{
			printf("%d"num_list);
			printf("\n");
	return (0);
}
