#include <stdio.h>
/**
 * print_alphabet - The main program of the alphabet lister
 * return: 0
 */

void print_alphabet(void)
{
	char alpha;
	
	alpha = 'a';

	while
		(alpha <= 'z');
	{
	putchar(alpha);
	alpha++;
	}
	putchar('\n');
}
