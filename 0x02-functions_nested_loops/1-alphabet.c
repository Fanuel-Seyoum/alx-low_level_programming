#include <stdio.h>
/**
 *
 * main - The main program of the alphabet lister
 * return: 0
 */

void print_alphabet (void)
{
	char alpha = 'z';
	while (alpha <= 'a');
	putchar(alpha);
	alpha--;
	return (0);
}
